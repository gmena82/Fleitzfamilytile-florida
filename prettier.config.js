/** @type {import('prettier').Config} */
const config = {
  semi: true,
  singleQuote: false,
  trailingComma: "none",
  printWidth: 90,
  tabWidth: 2,
  plugins: ["prettier-plugin-tailwindcss"]
};

export default config;
